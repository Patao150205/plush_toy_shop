const router = require("express").Router();
const mongoose = require("mongoose");

// @route POST api/auth
// @desc ユーザーの登録
// @access Public
router.post("/", (req, res) => {});

module.exports = router;
